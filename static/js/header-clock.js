function clock(){let d=new Date(), clock=document.querySelector('#tehran-clock'),gregorian=document.querySelector('#gregorian');if(clock)clock.textContent=d.toLocaleTimeString('fa-IR',{timeZone:'Asia/Tehran'});if(gregorian)gregorian.textContent=d.toLocaleDateString('en-CA',{timeZone:'Asia/Tehran'})}clock();setInterval(clock,1000)
