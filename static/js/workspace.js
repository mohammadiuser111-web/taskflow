const board=document.querySelector('#workspace-board');let actionData=null;board?.addEventListener('dragstart',e=>e.target.classList.add('dragging'));board?.addEventListener('dragend',e=>{let cards=[...board.querySelectorAll('.workspace-card')];cards.forEach((c,i)=>api('/api/workspace/move/',{kind:c.dataset.kind,id:c.dataset.id,position:i}));e.target.classList.remove('dragging')});board?.addEventListener('dragover',e=>{e.preventDefault();let d=board.querySelector('.dragging');let after=[...board.querySelectorAll('.workspace-card:not(.dragging)')].find(c=>e.clientY<c.getBoundingClientRect().top+c.offsetHeight/2);board.insertBefore(d,after||null)});function confirmAction(kind,action,id){actionData={kind,action,id};document.querySelector('#confirm-title').textContent=action==='complete'?'مطمئنی تمام کردی؟':'مطمئنی حذف شود؟';document.querySelector('#confirm-note').textContent=action==='complete'?'همه زیرکارها باید کامل شده باشند.':'این عمل قابل بازگشت نیست.';document.querySelector('#confirm-word').value='';document.querySelector('#confirm-modal').showModal()}async function runConfirm(e){e.preventDefault();let x=await api(actionData.kind==='task'?`/api/tasks/${actionData.id}/${actionData.action==='delete'?'delete':'status'}/`:`/api/projects/${actionData.id}/${actionData.action}/`,actionData.kind==='task'?{status:'done',confirm:document.querySelector('#confirm-word').value}:{confirm:document.querySelector('#confirm-word').value});if(x.ok)location.reload()}function editTask(id,title,description){let title2=prompt('عنوان',title),description2=prompt('توضیحات',description);if(title2!==null)api(`/api/tasks/${id}/edit/`,{title:title2,description:description2}).then(x=>x.ok&&location.reload())}function editProject(id,name,description){let n=prompt('عنوان پروژه',name),d=prompt('توضیحات',description);if(n!==null)api(`/api/projects/${id}/edit/`,{name:n,description:d}).then(x=>x.ok&&location.reload())}function extendTask(id){let v=prompt('زمان کمکی (مثال: 2026-08-04T18:30)');if(v)api(`/api/tasks/${id}/extend/`,{extension_due_date:v}).then(x=>x.ok&&location.reload())}
